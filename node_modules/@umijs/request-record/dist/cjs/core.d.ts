import { ConfigType } from "./config";
import "./utils";
export declare type CacheDataType = {
    query: Record<any, any>;
    res: Record<any, any>;
    payload: Record<any, any>;
};
export declare type CacheDataWithTypeType = CacheDataType & {
    types: string;
};
export declare type CacheType = {
    [key: string]: CacheDataWithTypeType;
};
export declare class Core {
    private options;
    private cache;
    static createCacheKey: (method: string, pathname: string) => string;
    constructor(options: {
        cacheFilePath: string;
        outputFilePath: string;
        mockOutputPath: string;
        mockCachePath: string;
        comment: boolean;
        namespace: string;
        mock: boolean;
        role?: ConfigType["role"];
    });
    add(cacheKey: string, data: CacheDataType): void;
    private onAdd;
    save(): void;
    getCache: () => CacheType;
    generate: () => void;
    generateMock: () => void;
}
